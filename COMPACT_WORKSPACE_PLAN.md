# 보람 RMS Lite 0.6.0 — 화면 밀도·배율·도움말·폴더 선택

상태: **Release 빌드 경고 0개·오류 0개, 합성 실행 시험 129개 통과·실패 0개**. 실제 렌더링한 작은 창·카드형 안내·폴더 선택 창도 확인했습니다. 사용자 설치본은 강제로 교체하지 않습니다. 배포본 생성·게시 결과는 아래 산출물로 별도 확인합니다.

## 요청 반영

| 기능 | 구현한 소스 동작 |
|---|---|
| 화면 배율 | 상단에서 80·85·90·95·100% 선택. 기존 RMS와 같은 범위, 기본 90%. 작업 기록과 별도 display.json에 저장. 메인·분리·안내·업데이트·폴더 선택 창에 반영. |
| 리네임 간격 | 구분선·제목·문구·체크·메모의 불필요한 위아래 공간 축소. 글자 잘림 방지를 위해 구좌수+이름 입력칸 62 높이와 세로 가운데 정렬 유지. |
| 신청서 목록 | 목록 행의 내부 최소높이 30→24, 행 내부 세로 여백 4→2. 제목·검색·필터·요약 사이 여백 축소. 스크롤 유지. |
| 사용 안내 | 빠른 시작 / 단축키·버튼 / 폴더·화면 / 저장·주의 4분류. 3단계 카드, 키캡, 화면 구성 그림과 버튼 차이 설명. |
| 여러 폴더 열기 | + 폴더 선택으로 상위 폴더 선택 → 목록 검색 → 필요한 폴더 체크 → 선택한 폴더만 탭으로 열기. 처음에는 모두 미선택. |
| 열린 폴더 전환 | 신청서 목록 위 작업 폴더 드롭다운. 탭과 같은 폴더로 전환하고 저장·선택 위치 유지. |

## 보호 조건

테마·배율 변경은 신청서 이름·상태·이미지를 수정하지 않습니다. 폴더 전환은 기존 저장 완료 확인을 사용하고, 실제 저장 실패 시 원래 폴더와 입력을 유지합니다. 전체 RMS 소스는 읽기 참조만 했으며 고객 자료를 시험에 사용하지 않습니다.

폴더 검색은 사용자가 선택한 경로 아래 최대 3단계, 방문 500개, 후보 200개로 제한합니다. 신청서 작업 폴더를 찾으면 그 내부의 카드 등의 하위 폴더를 별도 탭으로 중복 분리하지 않습니다. 제한이나 읽기 오류는 경고로 표시하며 전체 검색 완료로 가장하지 않습니다.

## 검수 결과

기존 기능 111개와 새 D01~D18을 합한 **129개 회귀시험 모두 통과**했습니다. 최초 실행 증거: `tests-data/v060/run_20260918_215445_877/SUMMARY.txt`.

추가 검수는 1024×720 논리 크기 창에서 5개 배율, 마지막 입력 항목 스크롤 접근, 폴더 전환과 저장 실패 보호, 원본 보존, 도움말 분류·줄바꿈, 4색 테마 조합을 확인했습니다. 생성한 화면 증거는 compact-main.png, compact-1024.png, guide-start.png, guide-shortcuts.png, folder-picker.png입니다. 모든 모니터·Windows 배율·NAS 환경을 시험한 것은 아닙니다.

## 배포 확인 방법

`release.py build`는 별도 `dist/releases/0.6.0`에 실행 파일을 만들고 동일 시험에 다시 통과해야 ZIP을 생성합니다. `BUILD_RESULT.json`으로 배포본 시험 수·압축 검사·해시를 확인합니다.

검토한 소스만 비공개 저장소 main에 올리고 `release.py publish`를 실행합니다. 기존 Release는 덮어쓰지 않습니다. 실제 게시 완료는 `PUBLISH_RESULT.json`의 태그·커밋·업로드 해시 검증으로 확인합니다.

이전 클라이언트의 `--update-probe`로 다운로드를 검수합니다. 이 검수는 설치 교체와 다르며, 사용자 앱·실제 신청서·사용자 설정을 강제로 교체하지 않습니다.
