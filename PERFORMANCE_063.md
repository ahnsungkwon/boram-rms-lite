# RMS Lite 0.6.3 — 성능·이름 복사 적용 기록

2026-09-20. 사용자의 Lite 성능 개선 및 신청서 이름만 클립보드에 복사 요청.

## 변경

- 3200px 긴 변 기준 미리보기 유지. 원본 이미지 파일을 수정하지 않는다.
- 선택/앞뒤 미리 읽기의 디코딩을 공유하고 메모리 캐시를 8개·128MiB로 제한한다. 디코딩 동시 작업은 2개다. 이 제한은 캐시 보관량이며 앱 전체 메모리 사용량 제한은 아니다.
- 축소 이미지와 썸네일은 독립 픽셀 버퍼를 보관해 큰 원본 프레임 참조를 해제한다.
- 파일명만 변경하면 캐시를 이전한다. 회전·압축·되돌리기·외부 파일 변경은 관련 캐시를 무효화한다.
- 체크 저장은 행 단위 바인딩 갱신으로 목록 전체 Reset을 제거한다. 이미지, 다중 선택, 확대 위치를 보존한다.
- 구좌 진행 표시 6개 컨트롤·누적 집계·색상 브러시를 재사용한다.
- 신청서 선택/이름 저장 시 저장된 이름만 복사한다. 예: `4홍길동.jpg` → `홍길동`. 별도 전화번호·생년월일 필드를 사용하지 않는다. 이름에 포함된 숫자/법인명은 임의로 추측해 삭제하지 않는다.
- 같은 신청서 상태 저장은 재복사하지 않는다. 수동 ‘이름 복사’ 버튼 제공. 클립보드 잠김은 60ms/120ms 간격으로 최대 3회 재시도하며 이전 신청서의 재시도가 새 이름을 덮지 않는다.

## 검증

- Release 빌드: 경고 0, 오류 0.
- 개발 실행본: **170개 통과, 실패 0**.
- 자체 포함 배포 실행본: **170개 통과, 실패 0**. ZIP CRC 및 설치 목록 파일 해시 검증 통과.
- 신규 PF01~PF08: 공유/동시 제한, 변경·회전 무효화, 캐시 용량, 취소된 요청, 디코드 실패, 독립 픽셀, 실제 창 전환·진행 표시.
- 신규 SP01~SP04: 브러시 공유, 파생 바인딩, 체크·리네임 시 목록 Reset 없이 행/미리보기/다중 선택 유지.
- 신규 NC01~NC07: 이름만 복사, 잠금 재시도, 순서 역전, 실패/취소, 실제 선택·저장 연결.
- 합성 4개 이미지에 요청 24회를 보내 실제 디코딩 **4회**, 이후 반복 요청 80회에서도 추가 디코딩 **0회**. 실행 시간 전체가 6배 빨라진다는 의미는 아니다.
- 모든 신청서 시험은 `tests-data`의 합성 파일과 격리 설정을 사용했다. 시험용 창은 화면 밖에서 비활성 표시하며, 실제 사용자 클립보드는 가짜 작성기로 대체했다.
- 개발 결과: `tests-data/lite-063-performance/run_20260920_210213_983/SUMMARY.txt`.

## 배포 범위

- 최초 작업에서는 로컬 실행본/ZIP만 적용했다. 이후 사용자의 정식 게시 승인에 따라 검증된 동일 ZIP과 `update-manifest.json`을 GitHub v0.6.3 Release에 게시한다. 실제 게시 상태는 해당 Release 페이지를 기준으로 확인한다.
- Lite 사용자 설정 및 신청서 파일은 교체 대상이 아니다. 기존 앱 파일은 백업한 뒤 검증된 새 앱을 적용한다.
- 적용 완료: `dist/BoramRMS_Lite/BoramRms.Lite.exe` 0.6.3.0. 앱을 새로 실행하면 적용된다. 작업 중인 다른 앱을 종료하지 않았다.
- 기존 로컬 실행본 0.3.1 보존: `dist/BoramRMS_Lite.before-0.6.3-20260920_2106`. 사용자 설정 3개 파일의 교체 전후 해시 동일.
- 배포 DLL SHA-256: `408140D100C9CAD04F7530668241B95F9B8553CDC3717F2EE0AB34F40569EC3C`.
- 로컬 ZIP: `dist/public/retry-1/releases/0.6.3/BoramRMS_Lite_0.6.3_win-x64.zip`.
- ZIP SHA-256: `277686321084ec25b6a890d51d24f057052087acd8093c74f6f52430a8c725a6`.
