# 업데이트 안내 — 0.3.0

## 처음 한 번

0.1.0에는 업데이트 기능이 없으므로 처음 한 번은 0.3.0 ZIP을 새 폴더에 풀어 실행해야 합니다. HomePC의 `START_LITE.cmd`는 새 고정 실행 경로 `dist\BoramRMS_Lite\BoramRms.Lite.exe`를 사용합니다. 이전 0.1.0 폴더는 보존합니다.

## 평소 사용

앱 시작 시 최신 정식 Release 정보를 한 번 확인합니다. 새 버전이 있으면 상단 버튼이 `업데이트 0.x.x`로 바뀝니다. 실행 중에도 `업데이트 확인`으로 최신 정보를 다시 조회할 수 있습니다. 다운로드나 설치를 몰래 실행하지 않습니다.

버튼 → 변경 내용 확인 → `업데이트 설치` → 설치 확인을 누르면 다운로드 및 검증 후 앱이 종료되고 새 버전으로 재실행됩니다. 기존 작업 탭과 선택 위치를 복원합니다. 파일명/상태의 미저장 입력, 리네임·리사이즈 등 진행 중 작업이 있으면 먼저 마쳐야 합니다.

인터넷이나 인증이 안 되면 업데이트만 중단됩니다. 이미지·리네임·리사이즈 등 로컬 기능은 계속 사용할 수 있습니다. 같은 설치 폴더에서 열린 다른 Lite 창은 직접 닫아야 합니다. 다른 RMS/서비스를 강제 종료하지 않습니다.

## 비공개 GitHub 인증

저장소 `ahnsungkwon/boram-rms-lite`와 Release는 비공개입니다. 소스나 업무 기능을 공개하지 않았습니다.

HomePC에서는 이미 로그인된 GitHub CLI 인증을 메모리에서 재사용합니다. 토큰을 화면·로그·환경 변수로 출력하거나 새 앱 패키지에 넣지 않습니다.

다른 PC에서는 업데이트 창의 `GitHub 인증 설정`에서 본인 계정의 fine-grained personal access token을 입력할 수 있습니다. 저장소 범위는 **boram-rms-lite 하나**, 권한은 **Contents: Read-only**로 제한하세요. 발급한 토큰을 채팅으로 보내지 말고 앱의 암호 입력칸에 직접 입력하세요. Windows 현재 사용자 DPAPI로 암호화하여 `%LOCALAPPDATA%\BoramRMSLite\github-read-token.dpapi`에 저장합니다. `저장 토큰 지우기`는 이 앱의 저장 인증만 삭제하며 GitHub CLI 로그아웃은 하지 않습니다.

읽기 권한이 없거나 만료된 토큰은 HTTP 401/403/404로 표시될 수 있습니다. 토큰 만료일과 저장소 접근 권한을 확인하세요.

## 보호 장치와 복구

다운로드는 지정한 GitHub 저장소의 Release와 허용된 GitHub HTTPS 호스트만 사용합니다. 인증 헤더는 해당 저장소 API 요청에만 붙고 CDN에는 보내지 않습니다. ZIP SHA-256, 크기, 제품명, 버전, 내부 파일별 해시를 검사합니다. 경로 이탈, 심볼릭 링크, 중복 파일명, 다른 프로그램 패키지와 다운그레이드를 거절합니다.

설치 폴더에 신청서·개인 문서·수정된 앱 파일이 섞여 있으면 자동 교체하지 않습니다. 앱 전용 폴더를 별도로 유지하세요. `.lite-install.json`은 설치 목록이므로 삭제/편집하지 마세요. 관리자 권한이 필요한 폴더보다 사용자가 쓸 수 있는 전용 폴더를 권장합니다.

이전 앱은 같은 상위 폴더에 `.backup-버전-식별자` 이름으로 보존합니다. 다운로드/검수는 `%LOCALAPPDATA%\BoramRMSLite\Updates` 아래에서 이루어집니다. 업데이트 중 실패하면 교체 전에는 기존 폴더가 유지되고, 교체 후 시작 확인 실패 시 이전 폴더 복구를 시도합니다. 복구까지 실패하면 백업 경로와 오류를 표시합니다. 전원 차단 등 모든 장애에 대해 자동 복구가 보장되는 것은 아닙니다. 임의로 백업을 지우지 마세요.

업데이트 준비 중에는 다운로드, 검증용 실행본, 같은 볼륨의 준비 폴더, 이전 앱 백업을 함께 보관하므로 여유 공간 약 1GB 이상을 확보하세요. 캐시/백업을 자동 정리하여 사용자 파일을 지우는 기능은 넣지 않았습니다.

이 버전은 Authenticode 서명된 설치 관리자가 아닙니다. HTTPS GitHub 계정 접근과 배포 해시 검증을 신뢰 기준으로 사용합니다. GitHub 계정 보안과 Release 게시 권한도 보호해야 합니다.

## 앞으로 새 버전을 배포할 때

1. 기능 수정 후 `BoramRms.Lite.csproj`의 Version을 올립니다. 같은 버전 파일을 바꿔치기하지 않습니다.
2. `python release.py build`를 실행해 배포본과 합성 검수를 생성합니다.
3. 변경된 소스/문서를 검토·커밋하고 Lite 저장소 main에 push합니다.
4. `python release.py publish`로 ZIP과 `update-manifest.json`을 Release에 올립니다.

**소스 push만으로 사용자 앱이 업데이트되지는 않습니다.** 검증된 실행 ZIP과 메타데이터를 갖춘 정식 Release를 게시해야 앱 버튼에서 새 버전으로 표시됩니다. 초안/사전 배포는 앱의 업데이트 대상이 아닙니다.

공식 참고: GitHub REST Releases/Release assets 문서의 `Contents: Read` 권한, asset API `application/octet-stream` 다운로드(200/302), GitHub CLI release create/edit를 사용합니다.
