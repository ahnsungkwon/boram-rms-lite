# 보람 RMS Lite 0.4 — 간결 작업 흐름

## 사용자가 하는 일

폴더 열기 → 이미지 확인 → 이름과 상태 수정 → 저장하고 다음.

- `저장하고 다음`과 이름 입력칸의 Space: 이름과 상태를 함께 저장한 뒤 다음 이미지로 이동합니다.
- `저장만`: 같은 내용을 저장하고 현재 이미지에 머뭅니다. 아래쪽 상태 저장 버튼도 같은 저장 동작입니다.
- `건너뛰기`: 저장 없이 이동합니다. 변경한 입력이 있을 때만 버릴지 확인합니다.
- 카드·보완·취소는 표시 상태입니다. 체크했다고 이미지를 다른 폴더로 옮기지 않습니다.
- 이름 뒤 숫자를 입력해도 구좌 숫자는 맨 앞으로 들어갑니다.
- 창/패널 크기가 달라지면 전체 이미지가 보이도록 맞춥니다. 다음/이전 이미지 선택만 바뀌면 확대·위치는 유지합니다.
- 삭제는 선택한 이미지만 확인 후 Windows 휴지통으로 이동합니다. 원본/보완 폴더 구분이나 동반 삭제는 없습니다.
- 300KB 미만 압축과 GitHub 업데이트는 독립 버튼으로 유지합니다.

## 프로그램 내부 원칙

1. 상태는 각 이미지가 있는 폴더의 `.rmslite` 안에 이미지별 JSON으로 저장합니다. 한 이미지의 상태 기록이 손상되어도 다른 이미지 작업을 막지 않습니다.
2. 이전 `.boramrms`의 로컬 상태는 초기 표시용으로만 읽습니다. 새 저장은 `.rmslite`에만 기록합니다. 이전 저널, 백업, 행사 TXT와 가공본을 변경하지 않습니다.
3. 리네임은 현재 이미지의 파일명만 바꿉니다. 새 이름 상태를 먼저 준비한 뒤 파일을 이동하므로 중단되어도 다른 파일까지 잠그지 않습니다. 충돌과 외부 변경은 해당 저장만 취소합니다.
4. 이름과 상태의 변경을 한 번에 저장합니다. 별도 상태 적용을 먼저 하라는 경고와 과거 저널 전역 차단을 사용하지 않습니다.
5. 같은 이름·상태이면 불필요한 기록을 쓰지 않습니다. 상태 파일은 임시 파일 검증 후 교체하고 이전 기록을 보존합니다.
6. 되돌리기는 이번 실행에서 이 폴더의 마지막 이름·상태·회전 작업만 대상으로 합니다. 과거 RMS 저널을 되돌리거나 강제 완료 처리하지 않습니다.
7. 위험한 삭제·원본 압축은 확인을 유지합니다. 일반 저장 성공과 과거 저널에 대해서는 팝업을 띄우지 않습니다.

## 검수해야 할 실제 흐름

기존 recovering 저널이 있는 폴더에서 상태 저장 → 이름+상태 저장 후 다음 → 창 축소 → 숫자 후입력 → 선택 이미지 휴지통 → 앱 재실행 후 상태 유지.

현재 E: 업무 폴더의 실제 복구 여부와는 별개입니다. 이전 저널을 삭제해 숨기는 방식이 아니라 새 Lite 작업을 기존 연동에서 분리합니다.
