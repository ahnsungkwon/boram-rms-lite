# 보람 RMS Lite 0.6.0 — 화면 배율·쉬운 안내·폴더 목록

0.6.3 추가: 신청서 선택 시 이름만 자동 복사합니다. 예: `4홍길동.jpg` → `홍길동`. ‘이름 복사’로 다시 복사할 수 있으며, 상태 체크만 할 때는 클립보드를 덮어쓰지 않습니다. 이미지·구좌 진행 표시·목록 행을 재사용해 반복 전환과 체크의 지연을 줄였습니다. 기존 작업 흐름은 유지합니다.

0.6.0 컴파일 및 합성 실행 시험 129개 통과·실패 0개를 확인했습니다. 새 버전을 설치해야 아래 동작이 적용됩니다.

## 기본 흐름

폴더 열기 → 이미지 확인 → 상태 체크(바로 저장) → Space(다음). 이름은 바꿀 때만 입력합니다. 비어 있으면 기존 파일명을 유지합니다.

이전 이미지는 Ctrl+Space입니다. 새 이름이 있으면 저장하고, 비어 있으면 기존 이름으로 이동합니다. 상태를 먼저 적용하라는 단계나 입력을 버릴지 묻는 일반 이동 팝업은 없습니다. 저장 실패는 현재 이미지·입력을 유지하면서 파일명과 원인을 하단에 표시합니다.

## 화면과 폴더

상단 화면 배율: 80·85·90·95·100%, 기본 90%. 선택은 별도 display.json에 저장합니다. 작은 화면에서는 줄인 세로 간격과 80~85% 배율, 스크롤을 함께 사용합니다. 이름 입력칸의 높이는 유지합니다.

여러 폴더 열기: 상위 폴더 선택 → 검색·체크 → 선택한 폴더만 탭으로 열기. 열린 폴더 전환: 신청서 목록 위 작업 폴더 드롭다운. 현재 저장을 마친 뒤 전환하며 선택 위치를 복원합니다. 검색은 최대 3단계·500개 방문·200개 후보이며 한도·읽기 오류를 표시합니다.

사용 안내: 빠른 시작 / 단축키·버튼 / 폴더·화면 / 저장·주의. 단계 카드·키캡·화면 구성 그림으로 확인합니다.

## 테마

상단 오른쪽 **테마** 메뉴에서 녹색·블루·보라·핑크를 즉시 선택합니다. 다음 실행에도 유지합니다. 작업 탭·입력·체크·이미지 확대·목록 선택을 바꾸지 않으며, 이미지 자체와 상태 의미색에는 적용하지 않습니다. 별도 `theme.json`에 저장하고 분리창·업데이트창에도 동기화합니다.

## 상태 저장

보완 사유와 기타 상태 체크·해제는 현재 이미지의 상태만 저장합니다. 이름 입력·커서·이미지 보기·목록 선택을 초기화하지 않습니다. 빠른 연속 체크는 클릭 순서대로 저장합니다. 이동·파일명 저장·종료는 이미 요청된 상태 저장을 기다립니다. 메모는 입력칸을 벗어나거나 이동할 때 저장합니다.

보완 사유·카드·주말미등록·추가·미성년자 등을 조합할 수 있습니다. 입력전취소/입력후취소 및 입력전변경/입력후변경은 각각 상호 대체됩니다. 기존 상태 데이터는 읽을 수 있도록 선택 상세를 추가 필드로 저장합니다.

## 버튼 설명

- 이전 폴더 열기: 지난번 폴더와 선택 이미지를 다시 열기. 데이터 되돌림 아님.
- 마지막 변경 취소: 현재 실행에서 현재 폴더의 마지막 이름·체크·메모·회전 1건 취소. 대상 파일·작업은 툴팁에 표시. 삭제·압축·업데이트 제외.
- 입력 원래대로: 항상 표시되는 일반 버튼. 저장 안 된 초안만 저장값으로 재읽기. 빈칸 이동에는 필요 없음. 완료된 자동 저장은 유지.
- 상태 다시 저장: 실제 쓰기 실패 때 오류 이유 바로 아래에 표시. 이름 입력 오류와 구분. 일반 작업에는 불필요.

## 보호 경계

`.rmslite` 이미지별 상태 저장을 사용하고 기존 공통 저널·TXT·가공본 수정에 의존하지 않습니다. 외부 변경·중복 이름·읽기 전용 파일은 보호합니다. 삭제와 원본 압축의 확인은 유지합니다. 실제 고객 자료는 개발 검수에서 사용하지 않습니다.

## 추가 시험

즉시 체크/해제, 6개 기타 상태, 연속 클릭, 이름 작성 중 체크, 저장 지연 중 이동, 양방향 키 이벤트, 직접 목록/탭 이동, 읽기 전용 실패와 재시도, 미저장 초안 취소, 마지막 저장 취소, 이전 폴더 열기 설명, 다중 선택 유지, 닫기 전 저장 등을 합성 자료로 시험했습니다. 빈 이름·Ctrl+Space의 B01~B12와 A01~A24 및 기존 워크플로·이미지·업데이트 시험 및 테마 T01~T16, 새 화면 D01~D18을 합해 129개 통과·실패 0개입니다.
